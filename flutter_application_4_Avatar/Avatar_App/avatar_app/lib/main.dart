import 'package:flutter/material.dart';

void main() {
  runApp(const Avatarpp());
}

class Avatarpp extends StatelessWidget {
  const Avatarpp({super.key});

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Avatar ',
      theme: ThemeData(
      ),
      
    );
  }
}

